# @package _global_
wandb:
  mode: online
  experiment: gfn_bo_mcl1

bo:
  n_iters: 1000
  checkpoint: true

run_name: "${hydra:runtime.choices.acqf_optimizer}_${hydra:runtime.choices.acquisition}"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      dataset: mcl1
      acquisition: ei,ucb
      acqf_optimizer: pool_maximizer,pool_sampler
      acqf_optimizer.max_batch_size: 512
      model: tanimoto_gp
      oracle: mcl1/mmgbsa
      seed: range(0,5)
